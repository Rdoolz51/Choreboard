import React from 'react';

//Profile page will be the page to add a child, add a reward or add a chore.
//Profile page will also return any chores/children or rewards added
//Profile page will not be displayed or accessed unless user is logged in

const Profile = () => {
    return (
        <div>
            
        </div>
    )
}

export default Profile;